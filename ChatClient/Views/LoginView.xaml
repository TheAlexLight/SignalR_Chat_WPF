<UserControl x:Class="ChatClient.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ChatClient.Views"
      xmlns:vm="clr-namespace:ChatClient.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Width="300">
            <TextBlock Text="Log in" HorizontalAlignment="Center" FontSize="36" />
            <TextBlock Text="Username:" Margin="0,5,0,0" />
            <TextBox  Padding="5" />
            <TextBlock Text="Password:" Margin="0,5,0,0" />
            <PasswordBox Padding="5" />
            <Button  Content="LOGIN" Padding="5" Margin="0,5,0,0"
                     Command="{Binding LoginCommand}"/>
            <Button  Content="Create new account" Padding="5" Margin="0,5,0,0"
                     Command="{Binding NavigateRegisterCommand}"/>

        </StackPanel>
    </Grid>
</UserControl>
