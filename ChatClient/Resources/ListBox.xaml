<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ChatClient.Models"
                    xmlns:converters="clr-namespace:ChatClient.Converters"
                    xmlns:extensions="clr-namespace:ChatClient.Extensions">

    <converters:MessageLineNormalizeConverter x:Key="normalizedMessage"/>

    <LinearGradientBrush x:Key="mainMessageBoxColors" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Color="#E6E7FF" Offset="0"/>
        <GradientStop Color="#F0F0FF" Offset="0.2"/>
        <GradientStop Color="#F0F0FF" Offset="0.4"/>
        <GradientStop Color="#F0F0FF" Offset="0.6"/>
        <GradientStop Color="#F0F0FF" Offset="0.8"/>
        <GradientStop Color="#E6E7FF" Offset="1"/>
    </LinearGradientBrush>

    <Style x:Key="usersBox" x:Name="usersListBox" TargetType="{x:Type ListBox}">
        <Setter Property="ItemsSource" Value="{Binding ActiveUsers}"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <!--<Setter Property="ContextMenu">
            <Setter.Value>
                <ContextMenu ItemsSource="{Binding ContextMenuActions}">
                    <ContextMenu.ItemContainerStyle>
                        <Style TargetType="{x:Type MenuItem}">
                            <Setter Property="Command" Value="{Binding Command}"/>
                            <Setter Property="CommandParameter" 
                                    Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}, Path=PlacementTarget.SelectedItem}"/>
                        </Style>
                    </ContextMenu.ItemContainerStyle>
                    <ContextMenu.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type local:UserContextMenu}" 
                                                  ItemsSource="{Binding MenuItems}">
                            <TextBlock Text="{Binding Header}"/>
                        </HierarchicalDataTemplate>
                    </ContextMenu.ItemTemplate>
                </ContextMenu>
            </Setter.Value>
        </Setter>-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <ScrollViewer extensions:ScrollViewerExtension.AlwaysScrollToEnd="True">
                        <Grid Background="{StaticResource mainMessageBoxColors}">
                            <ItemsPresenter/>
                        </Grid>
                    </ScrollViewer>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Border CornerRadius="25" 
                                    Background="CornflowerBlue" 
                                    Width="30" 
                                    Height="30"/>
                        <StackPanel>
                            <TextBlock Text="{Binding Username}"
                                       Foreground="{Binding UsernameColor}"
                                       Margin="10,0,10,0"
                                       FontSize="22"/>
                            <TextBlock Text="{Binding StatusMessage}"
                                   Margin="10,-5,10,0"
                                       FontStyle="Italic"
                                       Foreground="#5F5C5A"
                                       FontSize="16"
                                       FontWeight="SemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="activeUsers" TargetType="{x:Type ListBox}" BasedOn="{StaticResource usersBox}">
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu ItemsSource="{Binding ContextMenuActions}">
                                <ContextMenu.ItemContainerStyle>
                                    <Style TargetType="{x:Type MenuItem}">
                                        <Setter Property="Command" Value="{Binding Command}"/>
                                        <Setter Property="CommandParameter" 
                                    Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type ContextMenu}}, Path=PlacementTarget.SelectedItem}"/>
                                    </Style>
                                </ContextMenu.ItemContainerStyle>
                                <ContextMenu.ItemTemplate>
                                    <HierarchicalDataTemplate DataType="{x:Type local:UserContextMenu}" 
                                                  ItemsSource="{Binding MenuItems}">
                                        <TextBlock Text="{Binding Header}"/>
                                    </HierarchicalDataTemplate>
                                </ContextMenu.ItemTemplate>
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="messagesListBox" TargetType="{x:Type ListBox}" BasedOn="{StaticResource usersBox}">
        <Setter Property="ItemsSource" Value="{Binding Messages}"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden"/>
        <Setter Property="ItemTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Border CornerRadius="25" 
                                    Background="CornflowerBlue" 
                                    Width="30" 
                                    Height="30"/>
                        <StackPanel Width="400">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Path=UserInfo.Username}"
                                       Foreground="{Binding Path=UserInfo.UsernameColor}"
                                       Margin="10,0,5,0"
                                       FontSize="18"/>
                                <TextBlock Text="{Binding Path=Time}"
                                           FontSize="11"
                                           Margin="0,6,0,0"
                                           Foreground="#848484"
                                           FontWeight="SemiBold"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Message}"
                                       x:Name="message"
                                   Margin="10,-5,0,0"
                                       Foreground="Black"
                                       FontSize="20"
                                       MaxWidth="{Binding RelativeSource={RelativeSource AncestorType={x:Type ScrollViewer}}, Path=ActualWidth
                                                , Converter={StaticResource normalizedMessage}}"
                                       TextWrapping="Wrap"
                                       HorizontalAlignment="Left"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>